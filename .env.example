# ===========================================
# Auction Project - Environment Variables
# ===========================================
# Copy this file to the appropriate locations:
#   - auction-crawler/.env (for crawler)
#   - auction-viewer/.env.local (for Next.js frontend)

# --- Crawler Settings (auction-crawler/.env) ---
VWORLD_API_KEY=your-vworld-api-key-here
SKIP_VWORLD_API=false
PAGE_SIZE=40
BATCH_SIZE=50
REQUEST_DELAY=1.5
CONCURRENCY_LIMIT=1
MAX_RETRIES=3

# --- Frontend Settings (auction-viewer/.env.local) ---
JWT_SECRET=your-jwt-secret-here-change-this
ADMIN_USERNAME=your-admin-username
ADMIN_PASSWORD_HASH=your-sha256-password-hash
ADMIN_NAME=관리자

# To generate a password hash:
#   python3 -c "import hashlib; print(hashlib.sha256(b'your-password').hexdigest())"
